# flags.py — single in-memory flag store for bug toggles
# mirrors spec: payment, config, db, code, delivery
bug_flags = {
    "payment": False,
    "config": False,
    "db": False,
    "code": False,
    "delivery": False,
}
